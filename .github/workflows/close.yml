name: Close Polymarket Positions

on:
  workflow_dispatch:

jobs:
  close:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: 'pip' # Кэширование ускорит запуск в разы

      - name: Install dependencies
        # Прокси здесь удален специально: pip ставит через сеть GitHub без ошибок
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then 
            pip install -r requirements.txt 
          else 
            pip install py-clob-client requests python-dotenv
          fi

      - name: Test proxy
        env:
          HTTPS_PROXY: ${{ secrets.HTTP_PROXY }}
        run: |
          echo "=== Проверка IP через прокси ==="
          curl -x "$HTTPS_PROXY" -s https://ifconfig.me || echo "Прокси не отвечает"
          echo ""
          echo "=== Проверка связи с Polymarket ==="
          curl -I -x "$HTTPS_PROXY" -s https://clob.polymarket.com/ | grep "HTTP" || echo "API недоступно"

      - name: Clear debug log
        run: python -c "from pathlib import Path; p=Path('debug-7510a5.log'); p.unlink(missing_ok=True)"

      - name: Run Python bot
        env:
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
          WALLET: ${{ secrets.WALLET }}
          HTTP_PROXY: ${{ secrets.HTTP_PROXY }}
          HTTPS_PROXY: ${{ secrets.HTTP_PROXY }}
        run: python close.py

      - name: Print debug log (always)
        if: ${{ always() }}
        run: python -c "from pathlib import Path; p=Path('debug-7510a5.log'); print(p.read_text(encoding='utf-8') if p.exists() else 'Файл лога пуст')"

      - name: Upload debug log (always)
        if: ${{ always() }}
        uses: actions/upload-artifact@v4
        with:
          name: debug-7510a5-log
          path: debug-7510a5.log
          if-no-files-found: ignore
